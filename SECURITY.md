# Security Policy

Do not open issues with token values, OAuth callback URLs containing codes, or private account identifiers. Redact sensitive values and share only command output that does not expose credentials.

The default mode is dry-run. Live publishing requires `TIKTOK_DRY_RUN=false`.
